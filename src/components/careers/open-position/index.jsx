import MyButton from 'components/common/button/my-button';
import Enroll from 'components/for-marchents/enroll-now';

const OpenPosition = () => {
  return (
    <Enroll
      h2="We’re now remote-first. And we’re hiring.
  "
      button="See open position now"
    />
  );
};

export default OpenPosition;
